
{
  "entities": {
    "App": {
      "title": "App",
      "type": "object",
      "description": "Represents a mobile application available in the INR MINER store.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "publisher": { "type": "string" },
        "iconUrl": { "type": "string", "format": "uri" },
        "shortDescription": { "type": "string" },
        "description": { "type": "string" },
        "currentVersionId": { "type": "string" },
        "categoryIds": { "type": "array", "items": { "type": "string" } },
        "downloadCount": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "publisher", "downloadCount"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "User profile tracking balance and referral data.",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "balance": { "type": "number", "description": "Current balance in INR." },
        "ownReferralCode": { "type": "string", "description": "The user's unique code to share." },
        "referredBy": { "type": "string", "description": "The code used by this user when signing up." }
      },
      "required": ["uid", "email", "balance", "ownReferralCode"]
    },
    "DownloadRecord": {
      "title": "DownloadRecord",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "appId": { "type": "string" },
        "userId": { "type": "string" },
        "downloadedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/apps/{appId}",
        "definition": {
          "entityName": "App",
          "schema": { "$ref": "#/backend/entities/App" },
          "description": "App catalog."
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "User profile and balance info."
        }
      },
      {
        "path": "/downloadRecords/{recordId}",
        "definition": {
          "entityName": "DownloadRecord",
          "schema": { "$ref": "#/backend/entities/DownloadRecord" },
          "description": "Download logs."
        }
      }
    ]
  }
}
